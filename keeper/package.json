{
  "name": "launchnft-keeper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Harvests Pons fees, sweeps NFT floors via Seaport and runs raffles for LaunchNFT vaults",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "once": "node src/index.js --once",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@openzeppelin/merkle-tree": "^1.0.8",
    "dotenv": "^16.6.1",
    "viem": "^2.56.8"
  }
}
