# Runs the LaunchNFT keeper on a schedule, so no server is needed.
#
# Setup (all from github.com, works on a phone):
#   Settings → Secrets and variables → Actions
#     Secrets:   KEEPER_PRIVATE_KEY  (dedicated keeper wallet, gas money only)
#                OPENSEA_API_KEY     (optional; empty = auto free-tier key)
#                KEEPER_SOLANA_KEY   (optional; base58 secret key, only for Solana collections)
#                MAGICEDEN_API_KEY   (optional; needed to buy on Solana)
#     Variables (optional): LAUNCHER, START_BLOCK — default to the mainnet deployment below
#   Optional variables: MAX_CEILING_ETH, CEILING_MARKUP_BPS, MIN_HARVEST_ETH, DRY_RUN
#
# Raffle snapshots are committed to snapshots/, which Vercel serves with the site.
name: keeper

on:
  schedule:
    - cron: "*/30 * * * *" # every 30 min; GitHub may delay scheduled runs
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Simulate only (send no transactions)"
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: keeper
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: keeper/package-lock.json

      - name: Install
        working-directory: keeper
        run: npm ci

      - name: Run one keeper pass
        working-directory: keeper
        env:
          KEEPER_PRIVATE_KEY: ${{ secrets.KEEPER_PRIVATE_KEY }}
          OPENSEA_API_KEY: ${{ secrets.OPENSEA_API_KEY }}
          # Mainnet deployment; repo variables override these.
          LAUNCHER: ${{ vars.LAUNCHER || '0x4fbac0fe4ba373ea7c34661cb1b9934b6f4c5a37' }}
          START_BLOCK: ${{ vars.START_BLOCK || '70558619' }}
          # Ethereum / Base / Hyperliquid / Solana collections
          EXTERNAL_LAUNCHER: ${{ vars.EXTERNAL_LAUNCHER || '0xdbe1b07b2e5c4d4c32c4813c360ba3341f766270' }}
          KEEPER_SOLANA_KEY: ${{ secrets.KEEPER_SOLANA_KEY }}
          MAGICEDEN_API_KEY: ${{ secrets.MAGICEDEN_API_KEY }}
          COLLECTIONS_FILE: ../collections.json
          MAX_CEILING_ETH: ${{ vars.MAX_CEILING_ETH || '0.5' }}
          CEILING_MARKUP_BPS: ${{ vars.CEILING_MARKUP_BPS || '300' }}
          MIN_HARVEST_ETH: ${{ vars.MIN_HARVEST_ETH || '0.005' }}
          DRY_RUN: ${{ inputs.dry_run && '1' || vars.DRY_RUN || '0' }}
          SNAPSHOT_DIR: ../snapshots
        run: node src/index.js --once

      - name: Publish new raffle snapshots
        run: |
          if [ -n "$(git status --porcelain snapshots)" ]; then
            git config user.name "launchnft-keeper"
            git config user.email "keeper@users.noreply.github.com"
            git add snapshots
            git commit -m "keeper: publish raffle snapshots"
            git pull --rebase origin "$GITHUB_REF_NAME"
            git push origin "HEAD:$GITHUB_REF_NAME"
          fi
